#include "funciones.h"



int main()
{   
    // tactividad *actividad = (tactividad*)malloc(sizeof(tactividad));
    // actividad->datos.caminar.distancia = tomarDistancia();
    // actividad->datos.caminar.tiempo = tomarTiempo();

    // printf("tiempo de la actividad: %i\n", actividad->datos.arteMarciales.duracion);
    // printf("distancia de la actividad: %i", actividad->datos.arteMarciales.intensidad);
    tusuario *persona = (tusuario*)malloc(sizeof(tusuario));
    persona->actividades.



    int id = obtenerID();
    printf("%i", id); getchar();


    return 0;
}

